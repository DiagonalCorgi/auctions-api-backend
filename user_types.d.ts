type User = {
    userId?: number,
    email?: string,
    first_name?: string,
    last_name?: string,
    image_filename?: any,
    password?: string,
    currentPassword?: string,
    token?: string,
}